# 投研 Agent 项目面试提问清单

这份文档的目标不是背答案，而是帮你把这个项目讲得像你自己真的做过。

建议使用方式：

- 先自己口述回答
- 再对照项目代码补细节
- 最后把回答压缩成 1 分钟版和 3 分钟版

## 1. 这个项目解决什么问题？

你可以这样答：

> 这个项目解决的是投资分析师前期研究流程里大量重复、机械但又必须高质量完成的工作，例如公司动态搜集、监管文件定位、关键财务指标计算和报告初稿撰写。我用 CrewAI 搭了一个 3 Agent 的顺序工作流，让每个 Agent 负责自己最擅长的一段任务，把投研初稿生成流程自动化。

追问点：

- 为什么这个问题值得做自动化？
- 哪一步最耗时？
- 为什么不能只用单个 Agent？

## 2. 为什么要用 Multi-Agent，而不是一个大 Prompt？

建议答法：

> 我选择 Multi-Agent 是因为投研任务天然分阶段：外部信息搜集、财务分析、报告写作。单个大 Prompt 会把职责混在一起，调试困难，也不利于工具权限控制。拆成多个 Agent 后，每个 Agent 的 role、goal、tools 更清晰，任务失败时也能快速定位是哪一步出了问题。

你可以顺带提到：

- 可维护性更高
- Prompt 更短更聚焦
- 工具权限更容易最小化
- 中间产物更可观察

## 3. 这 3 个 Agent 分别干什么？

标准回答：

- `Information Gathering Analyst`
  - 负责 Google 搜索和 SEC 文件索引
- `Financial Statement Analyst`
  - 负责读取 SEC company facts、计算关键财务指标、解析本地 PDF 财报
- `Report Writing Analyst`
  - 负责整合前两位 Agent 的结果并生成投资备忘录

## 4. 为什么还要把财务指标逻辑单独写在 `finance.py`？

建议答法：

> 因为财务指标计算是确定性逻辑，不应该交给大模型“猜”。像毛利率、流动比率、自由现金流这些指标，应该由纯 Python 函数完成，再把结果交给 Agent 解释，这样精度更高，也更容易测试。

## 5. 你接了哪些工具？为什么这么设计？

你可以回答：

- `GoogleSearchTool`
  - 获取实时外部市场信息
- `SecFilingSearchTool`
  - 找最新 10-K / 10-Q
- `SecCompanyFactsTool`
  - 获取标准化财务事实
- `FinancialMetricsTool`
  - 计算财务指标
- `PDFTextExtractTool`
  - 抽取本地财报 PDF 文本
- `FileReadTool / FileWriteTool`
  - 保存和回读中间产物

然后补一句：

> 这些工具覆盖了“联网、监管数据、文件系统、结构化计算”四类核心能力，基本满足了投研自动化的最小闭环。

## 6. 为什么 API Key 都放到 `.env`？

建议答法：

> 这是最基础的工程规范。API Key 不能写死在代码里，应该从环境变量加载。这样更安全，也方便在本地开发、测试和部署环境之间切换。

## 7. 你怎么保证系统稳定性？

可以分 4 点讲：

- HTTP 请求统一使用带重试的 Session
- 对 `429` 和 `5xx` 做自动重试
- Agent 设置 `max_retry_limit=3`
- 每一步输出都落盘，方便回溯和人工兜底

补一句会更像工程师：

> 我没有只强调模型效果，而是优先做了失败可恢复和问题可定位，这对真实业务系统更重要。

## 8. 你怎么记录中间产物？

答法：

> 我给每个 Task 都配置了 `output_file`，会把每一步结果写到 `artifacts/` 目录，例如市场情报、SEC 文件梳理、财务分析都分别落盘。这样做有两个价值：一是方便排障，二是最终报告出现问题时能快速定位是哪一步的信息质量不足。

## 9. 你为什么选择顺序工作流？

建议答法：

> 因为这个场景的数据依赖是线性的。先有外部信息，再有监管文件，再做财务分析，最后写报告。顺序工作流比层级调度更简单、更稳定，也更适合新人理解。如果以后需要加人工审核或者分支逻辑，再升级到 Flow 会更合适。

## 10. 你为什么没有直接上 Flow？

可以这样说：

> Flow 更适合复杂状态机和条件分支，而这个项目的核心价值是先把投研初稿闭环跑通。当前版本用 Crew 的 sequential process 更直接。等后续要接入人工审批、缓存恢复、多公司批处理时，再升级到 Flow。

## 11. 你做了哪些测试？

你可以回答：

- 配置加载测试
- 财务指标计算测试
- Tool 输出格式测试
- Crew 结构测试

强化一句：

> 我不是只测 happy path，而是把最关键的确定性逻辑和结构性约束先锁住，这样后面重构工具层和 Prompt 时更安全。

## 12. 如果面试官说“这个效率提升 60%~80% 你怎么证明？”

建议答法：

> 这个数字是基于工作流拆解做的工程估算，不是宣称线上 AB 实验结果。一个分析师做一版基础投研初稿，通常会把时间花在网页检索、监管文件定位、财务指标整理和初稿结构化写作。我这个系统把这几步串起来后，可以显著减少重复劳动，所以给出的是保守的流程效率估计。若用于正式业务落地，我会补充任务计时和人工校对数据做更严谨的量化验证。

这个回答会比“我拍脑袋估的”成熟很多。

## 13. 如果面试官问“最难的点是什么？”

你可以答：

> 最难的点不是把 Agent 跑起来，而是把“模型推理”和“确定性工具能力”分层清楚。比如财务指标不能交给 LLM 自由发挥，必须拆到纯 Python 逻辑；而外部搜索和 SEC 数据又必须通过工具层封装，才能做到可测、可替换和可重试。

## 14. 如果面试官问“你这个项目哪里还能继续优化？”

推荐回答：

- 把最终报告改成 `Markdown + JSON` 双输出
- 引入 `output_pydantic` 做结构化下游消费
- 加缓存层减少重复外部调用
- 升级为 Flow，加入人工审核节点
- 接数据库做历史研究留存
- 增加多公司批处理

## 15. 如果面试官让你现场讲代码入口，从哪里开始？

你可以直接说：

1. 从 `main.py` 讲输入和运行入口
2. 再到 `settings.py` 讲配置来源
3. 再到 `crew.py` 讲 Agent / Task 编排
4. 然后到 `investment_tools.py` 讲外部工具
5. 最后到 `finance.py` 讲确定性财务计算

这条讲法会显得很清晰。

## 16. 一分钟项目介绍模板

> 我做了一个基于 CrewAI 的 Agent 驱动自动化投研系统。输入公司名和 ticker 后，系统会自动完成市场信息搜集、SEC 文件检索、财务指标计算和投资报告撰写。我把流程拆成了 3 个 Agent 和 4 个顺序任务，并接入了 Google Search、SEC API、PDF 解析、财务计算、文件读写等工具。为了保证工程可用性，我还做了配置集中管理、HTTP 重试、任务中间产物记录和自动化测试。这个项目的重点不是单纯调用 LLM，而是把多 Agent 协作、工具集成和可维护的软件结构真正落地。

## 17. 三分钟展开版提示

三分钟版本按这个顺序讲：

1. 业务背景
2. 为什么多 Agent
3. Agent 如何分工
4. 接了哪些工具
5. 如何做稳定性
6. 如何做测试
7. 最后讲效率提升和扩展方向

## 18. 面试前最后检查

你至少要能不看代码回答这 8 个问题：

- 为什么要拆成 3 个 Agent？
- 每个 Agent 的工具是什么？
- 为什么财务指标不让 LLM 直接算？
- 为什么要把 API Key 放 `.env`？
- 你的工作流为什么是顺序的？
- 你如何处理外部 API 不稳定？
- 中间产物存在哪里？
- 这个项目如果上线，你会继续补什么？
